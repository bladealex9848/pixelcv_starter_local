# assets

Este directorio debe contener `schema.json` oficial de RenderCV para validar YAML.

Descárgalo desde el repositorio oficial y colócalo como `backend/assets/schema.json`.
(En este paquete se omite para evitar desincronización; usa la versión vigente).
