# PixelCV Frontend (local)

Next.js + TailwindCSS para formularios y preview de CV.

## Pasos
```bash
cd frontend
npm install
cp .env.local.example .env.local
npm run dev
```
Navega a `http://localhost:3000`.
